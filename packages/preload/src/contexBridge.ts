// import {contextBridge, ipcRenderer} from 'electron';

// export function window() {
//   return contextBridge.exposeInMainWorld('electron', {
//     login: (channel: string, data: string) => ipcRenderer.send(channel, data),
//   });
// }
